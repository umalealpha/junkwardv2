import PlaceholderPage from '../../components/common/PlaceholderPage'

export default function BulkImportPage() {
  return (
    <PlaceholderPage
      title="DOM/COM Bulk Import"
      description="Bulk import DOM and COM policies via Excel. Upload a template file to create multiple policies at once."
    />
  )
}
